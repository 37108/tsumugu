# Normalize line endings to LF in the repository and in every working copy.
#
# GitHub's Windows runners default to core.autocrlf=true, which would check
# files out with CRLF. Prettier is configured with endOfLine: "lf", so without
# this a formatting check would fail on Windows for reasons unrelated to the
# change under review.
* text=auto eol=lf
