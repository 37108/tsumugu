# A minimal project

This directory contains one file, and that is enough: `tsumugu dev examples/minimal`
serves it at `/`.

There is no configuration file, and there is nothing to build. Add a second
Markdown file beside this one and it appears in the navigation.
