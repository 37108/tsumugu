/**
 * Version of the Tsumugu core package.
 *
 * `tests/cli.test.ts` asserts that this stays in sync with
 * `packages/core/package.json` by running the compiled CLI.
 */
export const version = "0.0.0";
